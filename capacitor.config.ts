import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-health-monitor',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
